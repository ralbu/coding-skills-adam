from pathlib import Path


def password_to_dict(file_name):
    users = {}
    return users


def test_passwd_to_dict_mine():
    
    assert dict["nobody"] == "-2"
    assert dict["_sandbox"] == "60"
    assert dict["_launchservicesd"] == "239"
